#!/bin/bash

export PATH="${PATH}:$HOME/.config/sxstatus/modules"

while true; do
  xsetroot -name "$(memory_sx) | $(brightness_sx) | $(volume_sx) | $(battery_sx) | $(clock_sx) | ☄ "
  sleep 1
done
